{
	"name": "domster",
	"version": "0.3.4",
	"description": "Feather-weight, blazing-fast DOM wrapper",
	"main": "dist/domster.min.js",
	"unpkg": "dist/domster.min.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"compile": "mkdir -p dist && uglifyjs src/core.js -b -o dist/domster.js && uglifyjs src/core.js -m -c -o dist/domster.min.js"
	},
	"repository": {
		"type": "git",
		"url": "git+https://murger@github.com/murger/domster.git"
	},
	"keywords": [],
	"author": {
		"email": "gmermer@gmail.com"
	},
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/murger/domster/issues"
	},
	"homepage": "https://github.com/murger/domster",
	"devDependencies": {
		"uglify-js": "^3.3.22"
	}
}
