{
	"name": "domster",
	"version": "0.3.11",
	"description": "Feather-weight, blazing-fast DOM wrapper",
	"main": "dist/domster.min.js",
	"scripts": {
		"test": "mocha test/bootstrap.js --recursive test",
		"build": "mkdir -p dist && uglifyjs src/core.js -b -o dist/domster.js && uglifyjs src/core.js -m -c -o dist/domster.min.js",
		"prepublish": "npm run build"
	},
	"files": [
		"dist"
	],
	"repository": {
		"type": "git",
		"url": "git+https://murger@github.com/murger/domster.git"
	},
	"keywords": [
		"browser",
		"dom"
	],
	"author": "Gürhan Mermer",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/murger/domster/issues"
	},
	"homepage": "https://github.com/murger/domster",
	"devDependencies": {
		"chai": "^4.1.2",
		"mocha": "^5.1.1",
		"puppeteer": "^1.3.0",
		"uglify-js": "^3.3.23"
	}
}
